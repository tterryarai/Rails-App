<%= form_for(@virtual_run) do |f| %>
  <% if @virtual_run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@virtual_run.errors.count, "error") %> prohibited this virtual_run from being saved:</h2>

      <ul>
      <% @virtual_run.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :last %><br>
    <%= f.text_field :last %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
