
<div class="command_buttons">
  <% case val %>
  <% when 'index' then %>
    <%= button_to 'New Author', {:action => 'new'}, :method => :get %>
  <% when 'new' then %>
    <%= button_to 'Back', {:action => 'index'}, :method => :get %>
  <% when 'edit' then %>
    <%= button_to 'Back', {:action => 'index'}, :method => :get %>
  <% else %>
    <%= button_to 'Back', {:action => 'index'}, :method => :get %>
  <% end %>
</div>

