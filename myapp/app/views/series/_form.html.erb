
<% if @series.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@series.errors.count, "error") %> prohibited this series from being saved:</h2>
    <ul>
    <% @series.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@series) do |f| %>

<div class="form">
  <div class="field">
    <%= f.label ts('series.form.realm') %><br />
    <%= f.select :realm_id, options_for_select(get_realm_list(), @series.realm_id) %>
  </div>
  <div class="field">
    <%= f.label ts('series.form.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit val %>
  </div>
</div>

<% end %>
