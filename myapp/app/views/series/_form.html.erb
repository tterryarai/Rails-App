
<% if @series.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@series.errors.count, "error") %> prohibited this series from being saved:</h2>
    <ul>
    <% @series.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@series) do |f| %>

<div class="form">
  <div class="field">
    <%= f.label ts('series.form.app_id') %><br />
    <%= f.text_field :app_id %>
  </div>
  <div class="field">
    <%= f.label ts('series.form.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit val %>
  </div>
</div>

<% end %>
