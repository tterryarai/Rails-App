
<div class="command_buttons">
  <% case val %>
  <% when 'index' then %>
    <%= button_to t('series.index.register'), {:action => 'new'}, :method => :get %>
  <% when 'new' then %>
    <%= button_to t('series.common.back'), {:action => 'index'}, :method => :get %>
  <% when 'edit' then %>
    <%= button_to t('series.common.back'), {:action => 'index'}, :method => :get %>
  <% else %>
    <%= button_to t('series.common.back'), {:action => 'index'}, :method => :get %>
  <% end %>
</div>

